% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_plays.R
\name{parse_plays}
\alias{parse_plays}
\title{Parse Retrosheet Play Event Notation}
\usage{
parse_plays(plays_data)
}
\arguments{
\item{plays_data}{Tibble of plays from \code{\link[=get_plays]{get_plays()}}, containing at least
an \code{event} column}
}
\value{
The input tibble with these columns appended:
\itemize{
\item \code{event_type}: one of \code{"single"}, \code{"double"}, \code{"triple"}, \code{"home_run"},
\code{"strikeout"}, \code{"walk"}, \code{"intentional_walk"}, \code{"hit_by_pitch"},
\code{"generic_out"}, \code{"fielders_choice"}, \code{"error"}, \code{"interference"},
\code{"foul_error"}, \code{"stolen_base"}, \code{"caught_stealing"}, \code{"pickoff"},
\code{"wild_pitch"}, \code{"passed_ball"}, \code{"balk"}, \code{"defensive_indifference"},
\code{"other_advance"}, \code{"no_play"} (\code{NA} if the string is unrecognized)
\item \code{is_plate_appearance}, \code{is_at_bat}, \code{is_hit}: batting-statistic flags
under standard scoring rules (sacrifices and catcher's interference
are plate appearances but not at bats)
\item \code{hit_value}: 0 for non-hits through 4 for a home run
\item \code{fielded_by}: the first fielder to touch the ball (position number),
\code{NA} when not recorded
\item \code{hit_location}: Retrosheet hit location code from the modifiers
(e.g. \code{"78"}, \code{"9LS"}), \code{NA} when not recorded
\item \code{trajectory}: \code{"ground"}, \code{"fly"}, \code{"line"}, or \code{"popup"}
\item \code{is_bunt}, \code{is_sac_fly}, \code{is_sac_hit}, \code{is_gdp}: modifier flags
\item \code{outs_on_play}, \code{runs_on_play}, \code{rbi}: outs recorded, runs scored, and
runs batted in on the play; RBI honors explicit \code{(RBI)}/\code{(NR)} markers
and otherwise follows standard scoring rules
\item \code{batter_dest}: base reached by the batter (0 = out or no advance,
1-3 = base, 4 = scored)
\item \code{runner1_dest}, \code{runner2_dest}, \code{runner3_dest}: destination of the
runners starting at each base, coded like \code{batter_dest}. These are
\code{NA} when the play does not mention the runner: event strings only
record movement, so a runner who holds is indistinguishable from an
empty base without full game-state tracking.
}
}
\description{
Interprets the Retrosheet event notation in the \code{event} column returned by
\code{\link[=get_plays]{get_plays()}} (e.g. \code{"S7/G56"}, \code{"64(1)3/GDP"}, \code{"K+SB2"}) and appends
typed columns describing each play: the event type, batting-statistic
flags, hit and fielding detail, modifier flags, and baserunner advancement.
}
\details{
The parser follows the event grammar documented at
\url{https://www.retrosheet.org/eventfile.htm}. An event string has up to three
sections: the basic play, \code{/}-separated modifiers, and \code{.}-separated runner
advances. Compound strikeout/walk events (\code{K+SB2}, \code{W+WP}) are classified
by their primary event with the secondary event reflected in the
baserunning columns. Pickoff-caught-stealing (\code{POCS}) plays are classified
as \code{"caught_stealing"}, matching how they are officially scored.

The parser works from the event string alone, without tracking game state
across plays, and is validated field-by-field against Chadwick's
\code{cwevent} reference implementation (see the package test suite). One
documented limit of string-level parsing: a runner from third scoring on
an error with two outs officially carries no RBI (rule 9.04(a)(3)), but
the out count is not knowable from the string, so the RBI is credited.
}
\examples{
\dontrun{
events <- get_events(year = 2024, type = "post")
plays <- get_plays(events) |>
  parse_plays()

# Batting outcomes are now typed columns
plays |>
  dplyr::count(event_type, sort = TRUE)
}

}
