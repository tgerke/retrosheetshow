% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_events.R
\name{parse_event_records}
\alias{parse_event_records}
\title{Parse Retrosheet Event Records into Structured Format}
\usage{
parse_event_records(events_raw, record_types = NULL)
}
\arguments{
\item{events_raw}{Tibble from \code{get_events()} containing raw parsed records}

\item{record_types}{Character vector of record types to include. If NULL
(default), includes all record types. Common types include:
\itemize{
\item \code{"id"} - Game ID
\item \code{"version"} - File format version
\item \code{"info"} - Game information (date, teams, site, etc.)
\item \code{"start"} - Starting lineups
\item \code{"play"} - Play-by-play events
\item \code{"sub"} - Substitutions
\item \code{"com"} - Comments
\item \code{"data"} - Additional data
}}
}
\value{
A tibble with parsed and structured event data
}
\description{
Takes a tibble of raw event records and parses them into a more structured
format with separate columns for different record types.
}
\examples{
\dontrun{
# Get raw events and parse them
events_raw <- get_events(year = 2024)

# Parse into structured format
events_parsed <- parse_event_records(events_raw)

# Get only play-by-play records
plays <- parse_event_records(events_raw, record_types = "play")
}

}
