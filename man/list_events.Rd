% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_events.R
\name{list_events}
\alias{list_events}
\title{List Available Retrosheet Event Files}
\usage{
list_events(year = NULL, type = "regular", check_availability = TRUE)
}
\arguments{
\item{year}{Numeric vector of years to check. If NULL (default), checks all
available years for the specified type. Can be a single year or a vector
of years.}

\item{type}{Character vector specifying the type(s) of events to list.
Options are:
\itemize{
\item \code{"regular"} - Regular season games (1911-2024)
\item \code{"allstar"} - All-Star games (1933-2024, with gaps)
\item \code{"post"} - Post-season games (1903-2024, with gaps)
Default is \code{"regular"}.
}}

\item{check_availability}{Logical. If TRUE (default), verifies that files
actually exist on Retrosheet servers. If FALSE, returns all years in the
theoretical range (faster but may include non-existent files).}
}
\value{
A tibble with columns:
\itemize{
\item \code{year} - The year of the data
\item \code{type} - The type of events ("regular", "allstar", or "post")
\item \code{url} - The URL to download the file
\item \code{available} - Logical indicating if the file exists (only if
\code{check_availability = TRUE})
}
}
\description{
Returns a tibble of available Retrosheet event files that can be downloaded.
This function checks which files are actually available on the Retrosheet
servers based on the specified criteria.
}
\examples{
\dontrun{
# List all available regular season files
list_events()

# List events for specific years
list_events(year = 2020:2024)

# List all types for recent years
list_events(year = 2023, type = c("regular", "allstar", "post"))

# Quick list without checking availability
list_events(year = 2020:2024, check_availability = FALSE)

# Use with get_events() in a pipe
list_events(year = 2024) |>
  get_events()
}

}
